export const SAVE_KEY = 'cat_cafe_save';

export const GAME_CONFIG = {
    initialCoins: 500,
    initialReputation: 0,
    maxCats: 35,
    maxCustomers: 5,
    customerSpawnIntervalMs: 8000,
    catMoodTickIntervalMs: 15000,
    firstCustomerDelayMs: 2000,
    initialAreaCapacities: { hall: 4, shelf: 3, window: 3 },
    notificationDurationMs: 3000,
    serveResultDelayMs: 1800,
    maxLogEntries: 50,
    visibleLogEntries: 8
};

export const AREA_NAMES = { hall: '大厅', shelf: '猫爬架', window: '窗边' };
